# conftest.py
# This file is placed in the project root to ensure pytest automatically adds
# the root directory to the Python search path (sys.path) during test execution,
# allowing imports like 'from packages.common import ...' to work correctly.
